# self-check report (regenerated by `make check`)
*_check_report.txt

# maintainer/owner tooling — kept locally, never shipped to participating teams
maintainer/

# R / editor / OS cruft
.Rhistory
.RData
.Rproj.user/
.DS_Store

# Python cruft
__pycache__/
*.pyc

# data
pipeline/data/CCES24_Common_OUTPUT_vv_topost_final.csv
pipeline/data/csv_pus.zip
pipeline/data/csv_hus.zip

# large raw third-party source archives -- kept locally, regenerable/re-extractable,
# not "our own pipeline code"; scripts/build_ate_archive.py, build_baseline_reference.py,
# and build_vlasceanu_validation.py document how to obtain and rebuild from these
pipeline/data/capsule-9843791-data.zip
pipeline/data/wv7c3-osfstorage-archive.zip
pipeline/data/data63.xlsx
pipeline/data/archive_70studies/
pipeline/data/climate_advocacy_megastudy/
# ATP1/ATP2 human_test.csv from https://github.com/skrsteski/survey-simulations
# @ faeb4e1a73567a8c98c69798774b63fdb27c79e1 -- kept locally, regenerable by
# re-cloning; scripts/prepare_g_atp_screen_manifest.py documents the source.
pipeline/data/atp_survey_simulations/

# third-party raw human-subjects data containing a respondent free-text PII
# field -- not redistributed here; publicly available at its authoritative
# source (OSF, DOI 10.17605/OSF.IO/JYNQH, filename data/final_bovitz_raw.csv,
# SHA-256 cbae3d4a7faf1027e0434d1af62b527dc82a8bf6b9b7935a469fb058a0ea65f6 --
# matches this repo's frozen protocol hash exactly). See
# pipeline/data/domain_validation/orchinik/README.md.
pipeline/data/domain_validation/orchinik/final_bovitz_raw.csv

# raw Together batch-request payloads -- kept locally only, exceed GitHub's 100MB
# per-file push limit at this project's scale. Fully deterministic: rebuildable
# byte-for-byte from inference.together_batch.prepare_batch (via iter_g_requests/
# iter_f_requests) given the frozen roster/config already in git. The sha256 of
# every one of these files is already recorded in the small, committed manifest/
# amendment/summary JSON files alongside it, so provenance/auditability is
# preserved even though the raw request text itself is not tracked.
pipeline/outputs/**/batch_input.jsonl
# the one request_manifest.csv that also exceeds the 100MB limit (the 136,000-row
# combined F calibration-production manifest); every smaller manifest stays in git.
pipeline/outputs/scientific_bakeoff/f_calibration_production/google_gemma-4-31B-it/request_manifest.csv
